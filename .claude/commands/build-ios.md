Build the AARU iOS app on the simulator and take a screenshot.

Steps:
1. Use XcodeBuildMCP `build_run_sim` to build and run AARU on iPhone 17 Pro
2. Wait for the app to launch
3. Take a screenshot using XcodeBuildMCP `screenshot`
4. Report build result (success/failure, warnings, errors)
5. Show the screenshot to verify the UI state
