Take a screenshot of the running iOS simulator and analyze the current UI state.

Steps:
1. Use XcodeBuildMCP `screenshot` to capture the current simulator screen
2. Display the screenshot
3. Describe what's visible: which screen (onboarding, world, conversation), character positions, any UI issues
